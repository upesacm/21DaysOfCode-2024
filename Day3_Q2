/*
Question 2:
Create a function to check if reversing the ASCII values of a string results in the same sequence when read backward. If true, return "symmetrical", otherwise return "not symmetrical".

Inputs:
A string.
Output:
"symmetrical" if the string meets the criteria, otherwise "not symmetrical".
Example:
string = "acxz"

Output: symmetrical

 */
public class Day3_Q2 {
    public static String isSymmetrical(String input) {
        // Convert the string to an array of ASCII values
        int[] asciiValues = new int[input.length()];
        for (int i = 0; i < input.length(); i++) {
            asciiValues[i] = (int) input.charAt(i);
        }

        // Check if the ASCII values array is symmetrical
        int n = asciiValues.length;
        for (int i = 0; i < n / 2; i++) {
            if (asciiValues[i] != asciiValues[n - i - 1]) {
                return "not symmetrical";
            }
        }
        return "symmetrical";
    }

    public static void main(String[] args) {
        String input = "acxz";
        String result = isSymmetrical(input);
        System.out.println(result);  
    }

}
